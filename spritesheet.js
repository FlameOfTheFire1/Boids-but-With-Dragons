class spriteSheet{
    images = []; //imageLoops
    hidden; 

    constructor(fileName){
        this.source = fileName;
    }

    getImage(rectangle){
        //rectangle gives the size of the sprite image

        return image;
    }
}